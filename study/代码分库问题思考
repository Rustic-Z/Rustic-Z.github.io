问题：
将web、api工程与底层的service、dao工程分成三个git仓库。
web工程pc端页面以及控制层的代码 与 api工程m端页面以及控制层代码互不影响
但是底层的service、dao层的代码又需要互相共同依赖。
这里各自建立三个git仓库来管理代码之后，会造成每次需要修改web、api工程时都必须同时拉取service、dao仓库的代码以及web或api仓库的代码。这样略麻烦，而且刚开始获取工程代码时又必须构建好工程之间引用路径的问题。

待解决。。。。
